export enum SocketRequestType {
    LOBBY_JOIN = 'joinLobby',
    LOBBY_JOIN_PUBLIC = 'joinLobbyPublic',
    LOBBY_TOGGLE_READY = 'toggleReady',
    LOBBY_SWITCH_CAR = 'switchCar',
    GAME_UPDATE = 'gameUpdate',
}
